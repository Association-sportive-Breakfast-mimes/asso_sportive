import React from 'react';
import Navigations from '../components/Navigations';

const Home = ( ) => {
    return (
        <div className = "home">
            <Navigations />
            <h1>Accueil</h1>
        </div>
    )
}

export default Home;